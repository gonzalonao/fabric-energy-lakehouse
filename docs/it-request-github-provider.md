# IT request — enable GitHub Git provider (student tenant)

Context: the ESESA/UCAM student tenant blocks the GitHub provider in Fabric Git
integration (phase-a Gotchas, 2026-07-13). This request asks the tenant admin to flip
**Admin portal → Tenant settings → Git integration → "Users can sync workspace items
with GitHub repositories"**. If granted, Track A can swap Azure DevOps → GitHub cheaply
(disconnect/reconnect the workspace, same `/fabric` folder).

**Status:** ☐ not sent · sent on: ______ · reply: ______
**To:** `info@esesa.eu` (no public IT address found; ESESA was acquired by UCAM, so the
tenant admin may be UCAM IT — check the domain of the student login and any onboarding
email footer for a better recipient).

---

**Asunto:** Solicitud: habilitar proveedor GitHub en la integración Git de Microsoft Fabric

Buenos días,

Soy antiguo alumno del Máster [nombre del programa] y dispongo de una cuenta
institucional de Microsoft 365 ([tu-usuario@dominio]). Estoy usando Microsoft Fabric
con la capacidad de prueba para un proyecto formativo de ingeniería de datos.

Al configurar la integración Git del área de trabajo, el proveedor GitHub aparece
deshabilitado («Contact an admin to allow this git provider»). ¿Sería posible que el
administrador del tenant habilite la opción **Admin portal → Tenant settings → Git
integration → "Users can sync workspace items with GitHub repositories"**? Puede
limitarse a un grupo de seguridad o únicamente a mi cuenta si se prefiere no aplicarlo
a toda la organización.

Es un cambio de configuración sin coste ni impacto para el resto de usuarios. Quedo a
disposición para cualquier aclaración.

Muchas gracias y un saludo,
Gonzalo López Crespo
